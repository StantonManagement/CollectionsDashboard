# ===========================================
# Collections Dashboard - Railway Environment Variables
# ===========================================
# This file documents all environment variables required for the Collections Dashboard
# Copy this file to .env.railway and fill in your actual values
# 
# Railway will automatically inject these variables into your service at runtime
# ===========================================

# ===========================================
# BACKEND SERVER CONFIGURATION
# ===========================================

# Server port (REQUIRED)
# Railway automatically sets this, but you can override if needed
# Default: 5000
PORT=5000

# Node environment (REQUIRED)
# Set to 'production' for Railway deployment
# Options: development, production
NODE_ENV=production

# ===========================================
# DATABASE CONFIGURATION
# ===========================================

# PostgreSQL Database URL (REQUIRED)
# Railway will provide this when you add a PostgreSQL database
# Format: postgresql://username:password@host:port/database
# Example: postgresql://postgres:password@containers-us-west-1.railway.app:5432/railway
DATABASE_URL=postgresql://username:password@host:port/database

# ===========================================
# DEVELOPMENT/REPLIT CONFIGURATION
# ===========================================

# Replit ID (OPTIONAL)
# Only used in development when running on Replit
# Used to enable Replit-specific Vite plugins
# Leave empty for Railway deployment
REPL_ID=

# ===========================================
# FRONTEND CONFIGURATION (Vite)
# ===========================================
# Note: Vite environment variables must be prefixed with VITE_
# These are exposed to the client-side code

# API Base URL (OPTIONAL)
# Base URL for API calls from the frontend
# Default: Uses relative URLs (recommended for Railway)
# VITE_API_BASE_URL=https://your-app.railway.app

# Application Title (OPTIONAL)
# Display name for the Collections Dashboard
# VITE_APP_TITLE=Collections Dashboard

# Environment (OPTIONAL)
# Frontend environment indicator
# VITE_NODE_ENV=production

# ===========================================
# RAILWAY-SPECIFIC NOTES
# ===========================================
# 
# 1. PORT: Railway automatically sets this to the correct port
# 2. DATABASE_URL: Add a PostgreSQL database in Railway dashboard
# 3. NODE_ENV: Set to 'production' for Railway deployment
# 4. All VITE_ variables are optional and only needed if you want to customize frontend behavior
# 5. The application uses in-memory storage by default, so DATABASE_URL is only needed for persistent data
# 
# ===========================================
# DEPLOYMENT CHECKLIST
# ===========================================
# 
# âœ… Set NODE_ENV=production
# âœ… Add PostgreSQL database in Railway dashboard
# âœ… Copy DATABASE_URL from Railway database settings
# âœ… Verify PORT is set (Railway handles this automatically)
# âœ… Test health check endpoint: /health
# 
# ===========================================
# HEALTH CHECK
# ===========================================
# 
# Railway will use the /health endpoint for health checks
# This endpoint returns: {"status": "ok"}
# Health check timeout: 100ms (configured in railway.json)
